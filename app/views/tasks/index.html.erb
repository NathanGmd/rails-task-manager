<h1>Mes Taches</h1>

<% @tasks.each do |task| %>
<div><%= link_to task.title, task_path(task) %></div>
<div><%= link_to "Delete", task_path(task), data: { turbo_method: :delete, turbo_confirm: "Are you sure ?"} %></div>
<% end %>
<div><%= link_to '+ Add a new task', new_task_path %></div>
